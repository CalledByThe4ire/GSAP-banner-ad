extends ../blocks/layout

block data

	- const page = { path: 'index', title: 'HTML Banner Ad using GSAP', description: 'training project', styles: [], scripts: ['https://cdnjs.cloudflare.com/ajax/libs/gsap/2.0.2/TweenMax.min.js'] };
